🧠 Prompt 名称：/summarize
────────────────────────────
请读取当前项目的全部关键文件（Python / JS / JSON / Config 等），
并自动生成一份名为 `system_summary.md` 的项目结构摘要，写入根目录。

────────────────────────────
📋 输出要求：
1️⃣ 文件结构概览（按目录层级）
   - 只展示项目主要文件，如：event_analyzer.py、fusion_engine.py、telegram_bot.py 等
   - 忽略虚拟环境、缓存文件、.env、__pycache__ 等无关内容

2️⃣ 模块说明
   - 每个核心文件写一句简短总结（主要功能 + 输入输出）
   - 例如：
     ```
     event_analyzer.py → 负责事件数据解析与市场趋势计算
     prompt_builder.py → 根据事件类别生成多模型提示词
     fusion_engine.py → 各模型预测融合与置信度加权
     ```

3️⃣ 核心函数概览
   - 对每个文件中主要函数写出函数签名与一句说明：
     ```
     def calculate_market_trend(price_history): 计算市场价格7天趋势
     def fetch_sentiment(keyword): 调用GDELT/NewsAPI获取舆情得分
     ```

4️⃣ 依赖项汇总
   - 罗列使用的外部API（如：Polymarket, Gamma, NewsAPI, Mediastack, GDELT）
   - 标注哪些模块依赖这些API

5️⃣ 模块交互图（文字版）
   - 用箭头描述数据流动，例如：
     ```
     Telegram → EventManager → EventAnalyzer → PromptBuilder → FusionEngine → Telegram Output
     ```

6️⃣ 程序入口说明
   - 标明程序从哪里启动（如 main.py / bot.py）

────────────────────────────
📤 输出格式示例：
────────────────────────────
# System Summary (v1.2)

## 📁 文件结构
- main.py
- event_analyzer.py
- prompt_builder.py
- fusion_engine.py
- telegram_bot.py
- config/base_weights_lmarena.json

## ⚙️ 模块说明
- event_analyzer.py → 负责市场趋势、舆情信号、规则解析
- prompt_builder.py → 动态生成多模型提示词（按事件类别）
- fusion_engine.py → 模型结果加权融合（固定 + 动态置信度）
- telegram_bot.py → 接收命令、返回AI预测结果

## 🔧 核心函数
- calculate_market_trend(price_history)
- fetch_sentiment(keyword)
- normalize_all_probabilities(predictions)
- build_prompts(event_category)

## 🔗 数据流
Telegram → EventManager → EventAnalyzer → PromptBuilder → FusionEngine → Telegram Output

## 🌐 外部依赖
- Polymarket API
- Gamma API
- NewsAPI / Mediastack / GDELT (舆情)
────────────────────────────
✅ 验收标准
────────────────────────────
- 生成文件名：system_summary.md
- 内容包含6个部分
- Markdown 格式清晰
- 中文说明简洁
────────────────────────────
